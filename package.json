{
  "name": "reef-protocol-monorepo",
  "private": true,
  "workspaces": [
    "protocol",
    "client",
    "directory"
  ],
  "scripts": {
    "build": "npm run build -w protocol && npm run build -w client && npm run build -w directory",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev:directory": "npm run dev -w directory"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.0",
    "prettier": "^3.8.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.56.0",
    "vitest": "^3.0.0"
  }
}
