# Reef Protocol Environment Variables

# XMTP environment: "dev" | "production"
REEF_XMTP_ENV=dev

# Reef config directory (default: ~/.reef)
REEF_CONFIG_DIR=

# Directory server URL (default: https://directory.reef-protocol.org)
# Set to http://localhost:3000 for local development
REEF_DIRECTORY_URL=

# Directory server
DATABASE_URL=postgres://reef:reef@localhost:5432/reef
PORT=3000
NODE_ENV=development

# XMTP DB encryption key (auto-generated if not set)
XMTP_DB_ENCRYPTION_KEY=
